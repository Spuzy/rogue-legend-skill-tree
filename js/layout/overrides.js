// Optional hand-tuned layout overrides. Add `nodeId: { x: number, y: number }`
// entries below. Anything you put here wins over the auto-layout.
export const LAYOUT_OVERRIDES = {
  adventure: {},
  specialization: {},
};
